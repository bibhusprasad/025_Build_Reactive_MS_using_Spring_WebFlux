spring:
  application:
    name: movies-info-service
  profiles:
    active: local
  config:
    activate:
      on-profile:
        - local
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local
